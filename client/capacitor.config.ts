import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sridattaenterprises.app',
  appName: 'SriDattaEnterprises',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
